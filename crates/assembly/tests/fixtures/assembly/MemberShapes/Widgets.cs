// projects_a_public_class_in_a_namespace: a dotted namespace must split into
// its component segments. Gadget lives two segments deep so the port pins the
// multi-segment split, not just a single-segment name.
namespace MemberShapes.Widgets;

public class Gadget
{
}
